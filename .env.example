# Only secrets belong here. Do not store tunables in .env.

# Shared secret for internal deployments (optional). Leave empty to disable.
SECURITY__API_KEY=

# Phase 2 (optional): Redis URL for training workers (treat as secret)
# REDIS_URL=redis://:password@redis:6379/0

